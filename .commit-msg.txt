feat: external video link + mobile responsive + Cloudflare-ready

- Replace 68.5MB recording video with GitHub Release external link
- Remove large video from git tracking (Cloudflare 25MB limit)
- Recording video now loads from GitHub Release CDN
- Mobile responsive: scrollable column layout with both phones visible
- Interactive demo on top, screen recording below on narrow screens
- Update .gitignore to exclude large video files
